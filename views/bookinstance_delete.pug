extends layout 

block content 
    h1 #{title} : #{book.title}

    p Do you really want to delete this Bookinstance?

    form(method='POST')
      div.form-group
        input#bookinstance.form-control(type='hidden', name='bookinstanceid', value=bookinstance._id )

      button.btn.btn-primary(type='submit') Delete